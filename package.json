{
  "name": "sora-netlify",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "node src/server/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.21.1",
    "serverless-http": "^3.2.0"
  }
}
